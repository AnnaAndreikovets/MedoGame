using System.Collections; using UnityEngine; using UnityEngine.SceneManagement; using UnityEngine.UI; using System.Collections.Generic;
public class LoadScreen : MonoBehaviour {public GameObject LoadingScreen; public Slider slider;
    /*public void Loading() { LoadingScreen.SetActive(true); StartCoroutine(); }
    IEnumerator LoadAsync() { AsyncOperation loadAsync = SceneManager.LoadScene(1);
        //Он поставил еденицу, а мне надо будет написать скрипт, 
        //которай будет принимать значение следующией сцены для загрузки
        //а если это будет меню, то мне надо будет загружать по значению
        //которое, у меня уже имеется
        loadAsync.allowSceneActivation = false;
        while (!loadAsync.isDone)
        { scale.va

        }
    }*/
}